---
declared-services:
  conversation-tutorial:
    label: conversation
    plan: free
applications:
- name: conversation-tutorial
  command: npm start
  path: .
  memory: 512M
  instances: 1
  services:
  - conversation-tutorial
  env:
    NPM_CONFIG_PRODUCTION: false
